[package]
name = "rucc-pp"
description = "Macro expansion, conditionals, include resolution, and the header cache."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

# Nothing yet. The crates this one is expected to reach for once M1 lands are
# `rucc-base`, `rucc-diag`, `rucc-session`, `rucc-gnu`, `rucc-lex`, all of lower rank than 5. Dependencies are added when they
# are used, not in advance, so that the graph `cargo xtask layers` reads is the real one.
[dependencies]
