[package]
name = "rucc-ir"
description = "The SSA IR with block parameters, and its printer, parser and verifier."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
readme.workspace = true

[lints]
workspace = true

# `rucc-target` and `rucc-session` are the two of lower rank than 8 this one does not use yet.
# Dependencies are added when they are used, not in advance, so that the graph
# `cargo xtask layers` reads is the real one.
[dependencies]
rucc-base.workspace = true
rucc-diag.workspace = true
