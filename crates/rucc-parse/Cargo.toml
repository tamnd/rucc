[package]
name = "rucc-parse"
description = "Recursive descent with a Pratt expression parser, declarators, and error recovery."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
readme.workspace = true

[lints]
workspace = true

# Ranks 0, 1, 4 and 6, all below this crate's rank 7. There is no `rucc-pp` here: the parser
# reads the tokens phase 7 produced and knows nothing about where they came from, which is what
# lets it be tested on a token stream written by hand.
[dependencies]
rucc-ast.workspace = true
rucc-base.workspace = true
rucc-diag.workspace = true
rucc-lex.workspace = true
