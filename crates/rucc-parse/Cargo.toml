[package]
name = "rucc-parse"
description = "Recursive descent with a Pratt expression parser, declarators, and error recovery."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

# Nothing yet. The crates this one is expected to reach for once M2 lands are
# `rucc-base`, `rucc-diag`, `rucc-ast`, `rucc-pp`, all of lower rank than 7. Dependencies are added when they
# are used, not in advance, so that the graph `cargo xtask layers` reads is the real one.
[dependencies]
