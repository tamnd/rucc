[package]
name = "rucc-lex"
description = "Translation phases 1 to 3, pp-tokens, and the fast scanner."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

# Ranks 0 and 1, both below this crate's rank 4. `rucc-session` will join them when the
# scanner starts reading dialect flags rather than taking its own `Options`.
[dependencies]
rucc-base = { path = "../rucc-base", version = "0.0.1" }
rucc-diag = { path = "../rucc-diag", version = "0.0.1" }
